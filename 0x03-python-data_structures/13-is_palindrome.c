#include <stdio.h>
#include <stdlib.h>
#include "lists.h"
/**
 *is_palindrome-checks if a singly linked list is a palindrome
 *@head:start of linked list
 *Return:0 if palindrame
**/
int is_palindrome(listint_t **head)
{
	if (head == NULL)
	return (0);
	else:
	return(1);
}
